<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="KeischProvisor.Pages.SubheaderDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KeischProvisor.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tk="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="40 20 40 10" Grid.Row="0" ChildrenTransitions="{StaticResource GeneralAnimations}" Orientation="Vertical" >
            <TextBlock x:Uid="SubheaderDetailPage_Title" x:Name="SubheaderDetailPage_Title" Text="Subheader Lists for Top Header {0}" FontWeight="SemiBold" FontSize="40"></TextBlock>
        </StackPanel>

        <ListView Grid.Row="1" Visibility="Visible" ItemsSource="{x:Bind subheaderIndices}" Padding="30 00 30 00" SelectionMode="None" ItemContainerStyle="{StaticResource DefaultListViewItemStyle_Catrified}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:SubheaderIndex">
                    <StackPanel>
                        <tk:SettingsCard Header="{x:Bind SettingsCardHeaderName}" Description="{x:Bind SettingsCardDescription}" Margin="0 0 0 2" IsClickEnabled="True" BorderThickness="0"></tk:SettingsCard>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
