<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="KeischProvisor.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KeischProvisor.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Menubar -->
        <Grid x:Name="MenuBarGrid" Grid.Row="0" Margin="0 -8 0 0" Visibility="Visible">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- leftside -->
            <MenuBar Grid.Row="0" Grid.Column="0">
                <MenuBarItem Title="File">
                    <MenuFlyoutItem Text="Open" />
                    <MenuFlyoutItem Text="Save" />
                    <MenuFlyoutItem Text="Exit" />
                </MenuBarItem>
                <MenuBarItem Title="Edit">
                    <MenuFlyoutItem Text="Cut" />
                    <MenuFlyoutItem Text="Copy" />
                    <MenuFlyoutItem Text="Paste" />
                </MenuBarItem>
                <MenuBarItem Title="View">
                    <MenuFlyoutItem Text="Status bar" />
                </MenuBarItem>
            </MenuBar>

            <!-- rightside -->
            <Button Height="32" Margin="0 0 4 0" Grid.Row="0" Grid.Column="1" Background="Transparent" BorderBrush="Transparent" Click="Button_Click" >
                <Grid>
                    <FontIcon FontSize="14" Glyph="&#xe713;"/>
                </Grid>
            </Button>
        </Grid>

        <!-- Main -->
        <Grid Grid.Row="1" Background="{ThemeResource LayerFillColorDefault}">
            
        </Grid>

        <!-- Status Bar-->
        <Grid  Visibility="Visible" x:Name="StatusBar" Grid.Row="2"  Height="32">
            <TextBlock x:Uid="MainPage_StatusBarTextBlock" x:Name="StatusBarText" VerticalAlignment="Center" Margin="8,0"/>
        </Grid>
    </Grid>
</Page>
